Write code that uses an algorithm to mutate 1bit genes ( like an image with 64 bit, )
save, delete, and regrow, until an image is grown.

and then evolve it based on bits.

Then train an AI to evolve an image.
